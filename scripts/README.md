# Scripts Folder

Contains **runnable scripts** for Sprint 1 pipelines.

## Current Scripts
- `fetch_tenders_with_coords.py` — Fetches tenders, matches to KMHFL facilities, assigns coordinates, and saves JSON output.

## How to Run
1. Activate your virtual environment:
```bash
source venv/bin/activate
Run the script:

python3 -m scripts.fetch_tenders_with_coords
